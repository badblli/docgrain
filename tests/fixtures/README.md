# Test fixtures

Only synthetic, redistributable documents belong here. Maintain fixtures for text PDFs, scanned PDFs, tables, embedded images, multi-column layouts, multilingual content and malformed files.

Expected extracted artifacts should be small and stable enough for regression testing.
