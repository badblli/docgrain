# Indexing package

This package coordinates optional embedding, vector-index and keyword-index writes. It must support idempotent upserts and version-aware removal without changing canonical document artifacts.
