# Observability package

This package will centralize structured logging, metrics and tracing conventions. All logs must include a correlation id and document/job/version identifiers when available; document body content must not be logged by default.
